# lok8s: Docker's SSH transport opens a new SSH connection per command.
# Default MaxSessions (10) and MaxStartups (10:30:100) cause connection
# resets during kind provisioning which runs many docker commands in
# rapid succession. Increase both limits.
MaxSessions 100
MaxStartups 100:30:200
