# E2E: no-services Tiltfile
load('../../../.lok8s/tilt/Tiltfile', 'lok8s')

# auto_cache_pull=False: no build:false services, nothing to pre-pull.
# Saves one local() subprocess.
lok8s(auto_cache_pull = False)
