# E2E: shared-registries Tiltfile. The scenario never starts Tilt; a
# project without a Tiltfile is not the shape under test.
load('../../../.lok8s/tilt/Tiltfile', 'lok8s')

lok8s(auto_cache_pull = False)
