# Rendered by lok8s from spec.oidc — apiserver StructuredAuthenticationConfiguration.
# apiserver.config.k8s.io/v1 — verified accepted by kube-apiserver v1.35.5.
apiVersion: apiserver.config.k8s.io/v1
kind: AuthenticationConfiguration
jwt:
  - issuer:
      url: "https://id.kubehz.dev"
      audiences:
        - "kubectl-client"
      certificateAuthority: |
        -----BEGIN CERTIFICATE-----
        MIIBfake
        -----END CERTIFICATE-----
    claimMappings:
      username:
        claim: "sub"
        prefix: "oidc:"
      groups:
        claim: "groups"
        prefix: "oidc:"
